[project]
name = "germanfuelprice-mcp"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.11"
dependencies = [
    "dotenv",
    "mcp[cli]>=1.27.0",
    "platformdirs",
    "pytest",
    "pytest-mock",
    "requests",
    "httpx",
]
